{# templates/result.html.twig #}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Résultat de la prédiction</title>

    {# Inclusion de Bootstrap via CDN #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Inclusion du fichier CSS personnalisé #}
    <link href="{{ asset('css/styles.css') }}" rel="stylesheet">
</head>
<body>
    <div class="result-container">
        <h1>Résultat de la prédiction</h1>
        <p>L'image téléchargée est de la classe : <span class="prediction">{{ prediction }}</span></p>
        <a href="{{ path('predict') }}" class="btn">Retour au formulaire</a>
        <a href="{{ path('home') }}" class="btn btn-secondary mt-3">Retour à l'accueil</a>
    </div>
</body>
</html>
